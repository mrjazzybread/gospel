(*@ function add (x : integer) (y : integer) : integer *)

(*@ function add_impl (x : integer) (y : integer) : integer =
      x + y *)

(*@ function hd (s : 'a sequence) : 'a *)

(*@ function hd_impl (s : 'a sequence) : 'a =
      s[0] *)

(*@ predicate is_empty (s : 'a sequence) *)

(*@ predicate is_empty_impl (s : 'a sequence) =
      s = Sequence.empty *)
